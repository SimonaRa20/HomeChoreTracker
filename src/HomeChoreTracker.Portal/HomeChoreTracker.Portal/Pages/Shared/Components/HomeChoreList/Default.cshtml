@model List<HomeChoreTracker.Portal.Models.Home.HomeResponse>

@if (Model.Count > 0)
{
	<li class="nav-item">
		@if (Model.Count == 1)
		{
			<a class="nav-link" href="~/HomeChores/@Model[0].Id">
				<i class="fa fa-list-alt" aria-hidden="true"></i>
				<span>Manage home chores</span>
			</a>
		}
		else
		{
			<a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#collapsePages"
			   aria-expanded="true" aria-controls="collapsePages">
				<i class="fa fa-list-alt" aria-hidden="true"></i>
				<span>Manage home chores</span>
			</a>
			<div id="collapsePages" class="collapse" aria-labelledby="headingPages" data-parent="#accordionSidebar">
				<div class="bg-white py-2 collapse-inner rounded">
					@foreach (var home in Model)
					{
						<a class="collapse-item" href="~/HomeChores/@home.Id">@home.Title</a>
					}
				</div>
			</div>
		}
	</li>
}
